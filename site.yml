---
- hosts: all
  remote_user: keith
  become: true
  roles:
    - common

- hosts: "!mailservers"
  remote_user: keith
  become: true
  roles:
    - dma

- hosts: buildservers
  remote_user: keith
  become: true
  roles:
    - buildserver
  tags:
    - buildserver

- hosts: dbservers
  remote_user: keith
  become: true
  roles:
    - dbserver
  tags:
    - dbserver

- hosts: mailservers
  remote_user: keith
  become: true
  roles:
    - acme
    - mailserver
  tags:
    - mailserver

- hosts: nameservers
  remote_user: keith
  become: true
  roles:
    - nameserver
  tags:
    - nameserver

- hosts: torrent
  remote_user: keith
  become: true
  roles:
    - torrent
  tags:
    - torrent

- hosts: webservers
  remote_user: keith
  become: true
  roles:
    - acme
    - webserver
  tags:
    - webserver

- hosts: xmpp
  remote_user: keith
  become: true
  roles:
    - acme
    - xmpp
  tags:
    - xmpp
