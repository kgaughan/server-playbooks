---
- name: install ntimed
  pkgng:
    name: ntimed
    cached: yes

- name: configure ntimed
  command: "sysrc ntimed_flags=\"{{ ntp_servers | join(' ') }}\""
  changed_when: false

- name: start ntimed
  service:
    name: ntimed
    enabled: yes
    state: started
